# Navora Update 2026-08-12 v2

This full merged project ZIP includes the previous authentication, OTP, Brevo, Express 5, and Google Auth updates, plus one regression-test alignment fix.

## v2 fix

- `backend/tests/frontend-e2e.test.js` now validates the actual current `map.html` heading (`Compare routes`) instead of the stale text `Plan an adaptive route`.
- No production secrets are included. Preserve your local `backend/.env` and `ai-service/.env`.
- Preserve installed runtime folders (`backend/node_modules`, `ai-service/.venv`) when applying this update to an existing local project.

## Local verification

Run from `backend`:

```powershell
npm audit
npm test
```

Run from `ai-service`:

```powershell
.\.venv\Scripts\python.exe -m pytest tests -q
```
