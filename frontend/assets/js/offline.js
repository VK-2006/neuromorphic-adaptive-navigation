const $=s=>document.querySelector(s);const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const recent=JSON.parse(localStorage.getItem('navora:recent-routes')||'[]');const prefs=JSON.parse(localStorage.getItem('navora:preferences')||'null');const last=JSON.parse(localStorage.getItem('navora:last-route-request')||'null');
if($('#offline-last-route'))$('#offline-last-route').innerHTML=last?`<strong>${esc(last.source?.label||'Source')}</strong> → <strong>${esc(last.destination?.label||'Destination')}</strong><br><small>Saved locally ${new Date(last.at).toLocaleString()}</small>`:'No last route is stored on this device.';
if($('#offline-recent'))$('#offline-recent').innerHTML=recent.length?recent.map(x=>`<div class="data-row"><strong>${esc(x.source?.label||'Source')}</strong> → ${esc(x.destination?.label||'Destination')}<br><small>${new Date(x.at).toLocaleString()}</small></div>`).join(''):'<div class="muted">No recent routes stored.</div>';
if($('#offline-settings'))$('#offline-settings').textContent=prefs?`Safety ${Math.round((prefs.safety||0)*100)}% · Traffic ${Math.round((prefs.traffic||0)*100)}% · Familiarity ${Math.round((prefs.familiarity||0)*100)}% · Theme ${localStorage.getItem('navora-theme')||'system'}`:`Theme ${localStorage.getItem('navora-theme')||'system'}; route preferences not stored yet.`;
