import{api,toast}from'./api.js';
const mapEl=document.getElementById('map');let map,routeLayers=[],hazardLayers=[],selected=null,selectedRoute=null,currentRoutes=[],sourceMarker,destMarker,searchTimers={},geocodingCaps={typeahead:false,effective:'nominatim'};const fallbackSource=[17.385,78.4867],fallbackDest=[17.4375,78.4483];
function init(){if(!mapEl||!window.L)return;map=L.map('map',{zoomControl:false}).setView(fallbackSource,12);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'© OpenStreetMap contributors'}).addTo(map);L.control.zoom({position:'bottomright'}).addTo(map);sourceMarker=L.marker(fallbackSource,{draggable:true}).addTo(map);destMarker=L.marker(fallbackDest,{draggable:true}).addTo(map);map.on('click',async e=>{destMarker.setLatLng(e.latlng);await syncField('destination',e.latlng)});sourceMarker.on('dragend',()=>syncField('source',sourceMarker.getLatLng()));destMarker.on('dragend',()=>syncField('destination',destMarker.getLatLng()));setCoords('source',...fallbackSource,'Current source');setCoords('destination',...fallbackDest,'Destination');syncField('source',sourceMarker.getLatLng());syncField('destination',destMarker.getLatLng());document.getElementById('route-form')?.addEventListener('submit',loadRoutes);document.getElementById('use-location')?.addEventListener('click',useLocation);document.getElementById('refresh-hazards')?.addEventListener('click',loadNearbyHazards);setupGeocoding();['safety','traffic','familiarity'].forEach(k=>{const el=document.getElementById(`${k}-pref`),out=document.getElementById(`${k}-value`);const paint=()=>out&&(out.textContent=`${el.value}%`);el?.addEventListener('input',paint);paint()})}
function setCoords(id,lat,lng,label){const el=document.getElementById(id);el.dataset.lat=Number(lat);el.dataset.lng=Number(lng);if(label)el.value=label}
async function syncField(id,ll){setCoords(id,ll.lat,ll.lng,`${ll.lat.toFixed(6)},${ll.lng.toFixed(6)}`);try{const place=await api(`/geocoding/reverse?lat=${encodeURIComponent(ll.lat)}&lng=${encodeURIComponent(ll.lng)}`);if(Math.abs(Number(document.getElementById(id).dataset.lat)-ll.lat)<1e-6)setCoords(id,ll.lat,ll.lng,place.label||`${ll.lat.toFixed(6)},${ll.lng.toFixed(6)}`)}catch{}}
async function setupGeocoding(){try{geocodingCaps=await api('/geocoding/status')}catch{}['source','destination'].forEach(bindAutocomplete)}
function bindAutocomplete(id){const input=document.getElementById(id),box=document.getElementById(`${id}-suggestions`);input?.addEventListener('input',()=>{delete input.dataset.lat;delete input.dataset.lng;clearTimeout(searchTimers[id]);const q=input.value.trim();if(q.length<2){box.innerHTML='';return}if(!geocodingCaps.typeahead){box.innerHTML='<div class="suggestion muted">Press Enter to search places</div>';return}searchTimers[id]=setTimeout(()=>searchPlaces(id,q),450)});input?.addEventListener('keydown',e=>{if(e.key==='Escape')box.innerHTML='';if(e.key==='Enter'&&!input.dataset.lat&&input.value.trim().length>=2){e.preventDefault();clearTimeout(searchTimers[id]);searchPlaces(id,input.value.trim())}})}
async function searchPlaces(id,q){const input=document.getElementById(id),box=document.getElementById(`${id}-suggestions`);try{const center=map.getCenter();const items=await api(`/geocoding/search?q=${encodeURIComponent(q)}&lat=${center.lat}&lng=${center.lng}&limit=6`);if(input.value.trim()!==q)return;box.innerHTML=items.map((x,i)=>`<button type="button" class="suggestion" role="option" data-i="${i}"><strong>${esc(x.name)}</strong><small>${esc(x.label)}</small></button>`).join('')||'<div class="suggestion muted">No matching places</div>';box.querySelectorAll('button[data-i]').forEach(b=>b.addEventListener('click',()=>choosePlace(id,items[Number(b.dataset.i)])))}catch(e){box.innerHTML=`<div class="suggestion muted">Search unavailable · ${esc(e.message)}</div>`}}
function choosePlace(id,p){const marker=id==='source'?sourceMarker:destMarker;marker.setLatLng([p.lat,p.lng]);setCoords(id,p.lat,p.lng,p.label||p.name);document.getElementById(`${id}-suggestions`).innerHTML='';map.setView([p.lat,p.lng],15)}
function useLocation(){navigator.geolocation?.getCurrentPosition(async p=>{const c=[p.coords.latitude,p.coords.longitude];sourceMarker.setLatLng(c);map.setView(c,16);await syncField('source',sourceMarker.getLatLng());toast(`Live GPS source set · ±${Math.round(p.coords.accuracy||0)} m`,'success')},e=>toast(`Location unavailable: ${e.message}`,'error'),{enableHighAccuracy:true,maximumAge:3000,timeout:15000})}
function parseInput(id){const el=document.getElementById(id),lat=Number(el.dataset.lat),lng=Number(el.dataset.lng);if(Number.isFinite(lat)&&Number.isFinite(lng))return{lat,lng};const parts=el.value.split(',').map(Number);if(parts.length===2&&parts.every(Number.isFinite)){setCoords(id,parts[0],parts[1],el.value);return{lat:parts[0],lng:parts[1]}}throw new Error(`Choose a ${id} suggestion or enter latitude,longitude`)}
async function loadRoutes(e){e?.preventDefault();try{const body={source:parseInput('source'),destination:parseInput('destination'),preferences:{safety:Number(document.getElementById('safety-pref')?.value||70)/100,traffic:Number(document.getElementById('traffic-pref')?.value||60)/100,familiarity:Number(document.getElementById('familiarity-pref')?.value||50)/100},simulation:document.getElementById('simulation')?.checked};const data=await api('/routes/compare',{method:'POST',body:JSON.stringify(body)});render(data.routes||[],data.recommendedRouteId,data.mode);const entry={source:{...body.source,label:document.getElementById('source').value},destination:{...body.destination,label:document.getElementById('destination').value},preferences:body.preferences,simulation:body.simulation,at:Date.now()};localStorage.setItem('navora:last-route-query',JSON.stringify(entry));const recent=JSON.parse(localStorage.getItem('navora:recent-routes')||'[]').filter(x=>x.source?.label!==entry.source.label||x.destination?.label!==entry.destination.label);recent.unshift(entry);localStorage.setItem('navora:recent-routes',JSON.stringify(recent.slice(0,8)));await loadNearbyHazards();toast(data.mode==='simulation'?'SIMULATION MODE routes loaded':'Road routes loaded')}catch(err){toast(err.message,'error')}}

async function loadNearbyHazards(){const host=document.getElementById('hazard-list');if(!host||!map)return;hazardLayers.forEach(x=>x.remove());hazardLayers=[];let center;try{center=parseInput('source')}catch{const c=map.getCenter();center={lat:c.lat,lng:c.lng}}try{const rows=await api(`/hazards/nearby?lat=${encodeURIComponent(center.lat)}&lng=${encodeURIComponent(center.lng)}&radius=5000`);host.innerHTML=rows.length?'':'<div class="empty-state">No nearby community hazards.</div>';for(const h of rows){const lat=Number(h.location?.coordinates?.[1]),lng=Number(h.location?.coordinates?.[0]);if(!Number.isFinite(lat)||!Number.isFinite(lng))continue;const marker=L.circleMarker([lat,lng],{radius:8,weight:3,fillOpacity:.55}).addTo(map);marker.bindTooltip(`${esc(h.type)} · ${esc(h.status)} · ${Math.round((h.trustScore||0)*100)}% trust`);hazardLayers.push(marker);const row=document.createElement('div');row.className='data-row';row.innerHTML=`<strong>${esc(h.type)}</strong><div class="muted">${esc(h.status)} · ${esc(h.snnRiskLevel||'UNKNOWN')} risk · ${Math.round((h.trustScore||0)*100)}% trust · ${h.nearbyConfirmations||0} nearby confirmation(s)</div><div class="toolbar"><button type="button" class="btn-navora btn-ghost" data-confirm="${esc(h._id)}">Confirm nearby</button></div>`;row.querySelector('[data-confirm]')?.addEventListener('click',()=>confirmHazard(h._id));host.appendChild(row)}}catch(e){host.innerHTML='<div class="muted">Sign in to load community hazards.</div>'}}
async function confirmHazard(id){if(!navigator.geolocation)return toast('Geolocation is required for a nearby confirmation','error');navigator.geolocation.getCurrentPosition(async pos=>{try{await api(`/hazards/${encodeURIComponent(id)}/confirm`,{method:'POST',body:JSON.stringify({confirmed:true,location:{lat:pos.coords.latitude,lng:pos.coords.longitude}})});toast('Nearby hazard confirmation recorded');await loadNearbyHazards()}catch(e){toast(e.message,'error')}},e=>toast(`Confirmation location unavailable: ${e.message}`,'error'),{enableHighAccuracy:false,maximumAge:60000,timeout:8000})}

function render(routes,recommendedId,mode){currentRoutes=routes;routeLayers.forEach(x=>x.remove());routeLayers=[];const list=document.getElementById('route-list');list.innerHTML='';const palette=['#7c7cff','#20c6b7','#ff9f43','#ff5f6d','#7f8c8d'];routes.forEach((r,i)=>{const line=L.polyline(r.coordinates.map(p=>[p.lat,p.lng]),{weight:r.id===recommendedId?8:5,opacity:r.id===recommendedId?0.95:0.65,color:palette[i%palette.length]}).addTo(map);routeLayers.push(line);if(r.congestedSegments?.length>1){const congestion=L.polyline(r.congestedSegments.map(p=>[p.lat,p.lng]),{weight:9,opacity:.55,color:'#d64545',dashArray:'4 6'}).addTo(map);routeLayers.push(congestion)}line.on('click',()=>select(r.id));const card=document.createElement('div');card.className='route-card'+(r.id===recommendedId?' selected':'');card.dataset.id=r.id;const tags=(r.routeTypes||[]).map(t=>`<span class="chip">${esc(t)}</span>`).join('');card.innerHTML=`<div style="display:flex;justify-content:space-between;gap:10px"><strong>${esc(r.label)}</strong><span>${Math.round(r.safetyScore)}% safe</span></div><div class="muted">${(r.distance/1000).toFixed(1)} km · ${Math.round(r.trafficDuration/60)} min · ${esc(r.trafficSeverity)}</div><div class="route-badges">${tags}<span class="chip">ACO ${Number(r.acoScore||0).toFixed(2)}</span><span class="chip">Familiar ${Math.round((r.familiarity||0)*100)}%</span><span class="chip">${esc((mode||r.mode||'unknown').toUpperCase())}</span></div>`;card.onclick=()=>select(r.id);list.appendChild(card)});if(routes[0])map.fitBounds(L.latLngBounds(routes.flatMap(r=>r.coordinates.map(p=>[p.lat,p.lng]))),{padding:[40,40]});select(recommendedId)}
function select(id){selected=id;selectedRoute=currentRoutes.find(r=>r.id===id)||null;document.querySelectorAll('.route-card').forEach(x=>x.classList.toggle('selected',x.dataset.id===id));if(selectedRoute){sessionStorage.setItem('selectedRouteId',id);if(selectedRoute.databaseId)sessionStorage.setItem('selectedRouteDbId',selectedRoute.databaseId);renderWhy(selectedRoute);renderSteps(selectedRoute)}}
function renderWhy(rec){const why=document.getElementById('why-route');if(!why)return;const arrival=new Date(Date.now()+rec.trafficDuration*1000).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'}),m=rec.explanation?.metrics||{};why.innerHTML=`<h3>WHY THIS ROUTE?</h3><div class="metric">${Math.round(rec.safetyScore)}%</div><div class="route-badges"><span class="chip">SNN ${Math.round((m.snnHazardRisk??rec.snnHazardRisk??0)*100)}%</span><span class="chip">Hazards ${Math.round((m.hazardExposure??rec.hazardExposure??0)*100)}%</span><span class="chip">DTW ${Math.round((m.dtwSimilarity??rec.dtwSimilarity??0)*100)}%</span><span class="chip">EMA safety ${Math.round((m.historicalSafety??rec.historicalSafety??0)*100)}%</span><span class="chip">ACO ${Number(rec.acoScore||0).toFixed(2)}</span></div><p>${(rec.explanation?.reasons||[]).map(x=>'✓ '+esc(x)).join('<br>')}</p><p class="muted">ETA ${Math.round(rec.trafficDuration/60)} min · Arrival ${arrival} · Delay ${Math.round(rec.trafficDelay/60)} min · Traffic ${esc(rec.trafficMode||'unknown')}</p><div id="turn-by-turn"></div><div class="toolbar"><button id="begin-selected-journey" class="btn-navora" type="button">Start selected journey</button></div>`;document.getElementById('begin-selected-journey')?.addEventListener('click',beginJourney)}
function renderSteps(route){const host=document.getElementById('turn-by-turn');if(!host||!route)return;const steps=(route.steps||[]).slice(0,8);host.innerHTML=steps.length?`<h4>Turn-by-turn</h4>${steps.map((s,i)=>`<div class="route-card"><strong>${i+1}. ${esc(s.maneuver?.instruction||[s.maneuver?.type,s.maneuver?.modifier].filter(Boolean).join(' ')||'Continue')}</strong><div class="muted">${esc(s.name||s.maneuver?.name||'')} · ${Math.round(s.distance||0)} m</div></div>`).join('')}`:'<p class="muted">Turn instructions are unavailable from this provider for this route.</p>'}
async function beginJourney(){if(!selectedRoute)return toast('Select a route first');try{const source=parseInput('source'),destination=parseInput('destination');if(!selectedRoute.databaseId)throw new Error('Sign in and calculate routes again before starting a persisted journey');const j=await api('/journeys',{method:'POST',body:JSON.stringify({routeId:selectedRoute.databaseId,mode:selectedRoute.mode==='simulation'?'SIMULATION':'LIVE',source,destination})});sessionStorage.setItem('journeyId',j._id);await api(`/journeys/${j._id}/start`,{method:'POST'});location.href='journey.html'}catch(e){toast(e.message.includes('Authentication')?'Sign in before starting a saved journey.':e.message,'error')}}
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
init();
