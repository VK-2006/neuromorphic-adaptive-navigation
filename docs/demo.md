# Demo Guide

Recommended classroom path: open website → registration/OTP architecture → login → dashboard → source/destination → simulation candidate routes → simulated traffic label → CRM/ACO explanation → live journey simulation → distance covered/remaining → voice → optional camera → detector/SNN fallback label if no model weights → hazard/geofence/reroute → complete journey → CRM/EMA update → replay → World Chat → SOS architecture → admin verification. Never describe simulation as live data.
