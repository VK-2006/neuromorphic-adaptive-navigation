# Accessibility

The UI uses semantic form labels, focusable controls, responsive touch targets, theme contrast variables and `prefers-reduced-motion`. Production verification should include WCAG 2.2 keyboard navigation, screen-reader labels, modals/toasts, chart alternatives and map interaction alternatives.
