const {dtwSimilarity}=require('../src/services/dtw');const {ema}=require('../src/services/ema');const {optimize}=require('../src/services/aco');const {explain}=require('../src/services/explainabilityService');const weights={distance:.15,time:.15,traffic:.15,safety:.25,familiarity:.12,history:.10,preference:.08};test('DTW identical route is highly similar',()=>{const a=[{lat:1,lng:1},{lat:2,lng:2}];expect(dtwSimilarity(a,a)).toBeGreaterThan(.99)});test('EMA formula',()=>expect(ema(.5,1,.3)).toBeCloseTo(.65));test('ACO ranks safer reliable route when tradeoff is reasonable',()=>{const routes=[{id:'short',distance:1000,trafficDuration:100,trafficDelay:10,safetyScore:45,familiarity:.2,historicalSafety:.4},{id:'safe',distance:1120,trafficDuration:108,trafficDelay:8,safetyScore:94,familiarity:.8,historicalSafety:.9}];const r=optimize(routes,{ants:50,iterations:60,weights,seed:4});expect(r.selected.id).toBe('safe');expect(r.selected.acoScore).toBeGreaterThan(0)});test('explanation uses calculated inputs',()=>{const r={id:'a',distance:1100,trafficDuration:100,trafficDelay:5,safetyScore:92,hazardExposure:.1,trafficSeverity:'LIGHT',familiarity:.8,historicalSafety:.9,acoScore:.91};const e=explain(r,[r,{...r,id:'b',distance:1000,safetyScore:50}]);expect(e.reasons.join(' ')).toMatch(/ACO|safety/i)})
