const request=require('supertest');process.env.NODE_ENV='test';const {createApp}=require('../src/app');const app=createApp();test('register validates email/password before DB use',async()=>{const r=await request(app).post('/api/v1/auth/register').send({name:'A',email:'bad',password:'1'});expect(r.status).toBe(422)});test('protected profile requires auth',async()=>{const r=await request(app).get('/api/v1/users/me');expect(r.status).toBe(401)});
