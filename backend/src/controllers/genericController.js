const Device=require('../models/Device');const RouteMemory=require('../models/RouteMemory');const Notification=require('../models/Notification');const TrustedContact=require('../models/TrustedContact');const User=require('../models/User');const Journey=require('../models/Journey');const Hazard=require('../models/Hazard');const {ok}=require('../utils/response');const pick=(obj,keys)=>Object.fromEntries(keys.filter(k=>obj?.[k]!==undefined).map(k=>[k,obj[k]]));const DEVICE_FIELDS=['name','deviceType','externalId','battery','capabilities','enabled'];const CONTACT_FIELDS=['name','phone','email','relationship','sharePermission'];
exports.profile=async(req,res)=>ok(res,{id:req.user._id,name:req.user.name,email:req.user.email,role:req.user.role,preferences:req.user.preferences,avatarUrl:req.user.avatarUrl,emailVerified:req.user.emailVerified});exports.updateProfile=async(req,res)=>{for(const k of ['name','preferences'])if(req.body[k]!=null)req.user[k]=req.body[k];await req.user.save();ok(res,{id:req.user._id,name:req.user.name,email:req.user.email,role:req.user.role,preferences:req.user.preferences})};
exports.dashboard=async(req,res)=>{const uid=req.user._id;const [memories,journeys,notifications]=await Promise.all([RouteMemory.find({userId:uid}).sort({lastTravelledAt:-1}).lean(),Journey.find({userId:uid}).sort({createdAt:-1}).limit(50).lean(),Notification.countDocuments({userId:uid,readAt:null})]);const completed=journeys.filter(j=>j.status==='COMPLETED');const safety=completed.map(j=>Math.max(0,100*(1-(j.averageRisk||0))));const verifiedAvoided=completed.reduce((s,j)=>s+(j.decisionEvents||[]).filter(e=>e.type==='REROUTE_ACCEPTED').length,0);ok(res,{metrics:{safetyTrend:safety.length?Math.round(safety.reduce((a,b)=>a+b,0)/safety.length):null,routeMemories:memories.length,successfulJourneys:completed.filter(j=>j.success!==false).length,verifiedHazardsAvoided:verifiedAvoided,unreadNotifications:notifications},trend:completed.slice(0,12).reverse().map((j,i)=>({label:`J-${i+1}`,safety:Math.round(100*(1-(j.averageRisk||0))),risk:j.averageRisk||0,date:j.completedAt||j.updatedAt})),recentJourneys:journeys.slice(0,5),recentMemories:memories.slice(0,5)})};
exports.devices=async(req,res)=>ok(res,await Device.find({userId:req.user._id}).sort({updatedAt:-1}));exports.addDevice=async(req,res)=>{const body=pick(req.body,DEVICE_FIELDS);const d=await Device.findOneAndUpdate({userId:req.user._id,externalId:body.externalId||undefined},{$set:{...body,userId:req.user._id,lastSeenAt:new Date()}},{upsert:true,new:true,setDefaultsOnInsert:true});req.app.get('io')?.to(`user:${req.user._id}`).emit('device:updated',d);ok(res,d,'Device saved',201)};exports.updateDevice=async(req,res)=>{const d=await Device.findOneAndUpdate({_id:req.params.id,userId:req.user._id},{$set:{...pick(req.body,DEVICE_FIELDS),lastSeenAt:new Date()}},{new:true});if(!d)return res.status(404).json({success:false,message:'Device not found'});req.app.get('io')?.to(`user:${req.user._id}`).emit('device:updated',d);req.app.get('io')?.to(`device:${d._id}`).emit('device:updated',d);ok(res,d)};exports.deleteDevice=async(req,res)=>{const d=await Device.findOneAndDelete({_id:req.params.id,userId:req.user._id});if(d)req.app.get('io')?.to(`user:${req.user._id}`).emit('device:deleted',{deviceId:String(d._id)});ok(res,{deleted:!!d})};
exports.memory=async(req,res)=>ok(res,await RouteMemory.find({userId:req.user._id}).sort({lastTravelledAt:-1}));exports.notifications=async(req,res)=>ok(res,await Notification.find({userId:req.user._id}).sort({createdAt:-1}).limit(100));exports.readNotification=async(req,res)=>{const n=await Notification.findOneAndUpdate({_id:req.params.id,userId:req.user._id},{$set:{readAt:new Date()}},{new:true});ok(res,n)};
exports.contacts=async(req,res)=>ok(res,await TrustedContact.find({userId:req.user._id}));exports.addContact=async(req,res)=>ok(res,await TrustedContact.create({...pick(req.body,CONTACT_FIELDS),userId:req.user._id}),'Contact added',201);exports.updateContact=async(req,res)=>{const c=await TrustedContact.findOneAndUpdate({_id:req.params.id,userId:req.user._id},{$set:pick(req.body,CONTACT_FIELDS)},{new:true});if(!c)return res.status(404).json({success:false,message:'Contact not found'});ok(res,c)};exports.deleteContact=async(req,res)=>{const c=await TrustedContact.findOneAndDelete({_id:req.params.id,userId:req.user._id});ok(res,{deleted:!!c})};exports.users=async(req,res)=>ok(res,await User.find().select('-passwordHash').limit(200));