const crypto=require('crypto');exports.randomOtp=()=>String(crypto.randomInt(100000,1000000));exports.sha256=v=>crypto.createHash('sha256').update(String(v)).digest('hex');exports.randomToken=(bytes=32)=>crypto.randomBytes(bytes).toString('base64url');exports.timingSafeEqualHash=(raw,hash)=>{const a=Buffer.from(exports.sha256(raw));const b=Buffer.from(hash);return a.length===b.length&&crypto.timingSafeEqual(a,b)};
