const mongoose=require('mongoose');const schema=new mongoose.Schema({messageId:{type:mongoose.Schema.Types.ObjectId,ref:'ChatMessage',required:true,index:true},reporterId:{type:mongoose.Schema.Types.ObjectId,ref:'User',required:true},reason:{type:String,required:true,maxlength:500},status:{type:String,enum:['PENDING','REVIEWED','DISMISSED','ACTIONED'],default:'PENDING'}},{timestamps:true});module.exports=mongoose.model('ChatReport',schema);
