const logger=require('../config/logger');
function classify(path,status){const p=path.toLowerCase();if(p.includes('/auth/login'))return status<400?'login':'failed_login';if(p.includes('/auth/google'))return 'google_auth';if(p.includes('verify-email')||p.includes('verification')||p.includes('otp'))return 'otp_operation';if(p.includes('forgot-password')||p.includes('reset-password'))return 'password_reset';if(p.includes('/routes/reroute'))return 'rerouting';if(p.includes('/routes'))return 'routes';if(p.includes('/traffic'))return 'traffic';if(p.includes('/journeys'))return 'journeys';if(p.includes('/tracking'))return 'gps_tracking';if(p.includes('/hazards'))return 'hazards';if(p.includes('/devices'))return 'devices';if(p.includes('/memory'))return 'crm';if(p.includes('/chat'))return 'chat';if(p.includes('/sos'))return 'sos';if(p.includes('/admin'))return 'admin_operation';return 'api_request'}
module.exports=function activityLogger(req,res,next){const started=Date.now();res.on('finish',()=>{const event=classify(req.path,res.statusCode);const level=res.statusCode>=500?'error':res.statusCode>=400?'warn':'info';logger[level]({event,method:req.method,path:req.baseUrl+req.path,status:res.statusCode,durationMs:Date.now()-started,userId:req.user?._id?String(req.user._id):undefined})});next()};
