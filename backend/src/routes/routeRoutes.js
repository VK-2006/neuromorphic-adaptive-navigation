const r=require('express').Router();const c=require('../controllers/routeController');const v=require('../validators/routeValidators');const dv=require('../validators/domainValidators');const validate=require('../middleware/validate');const {optional,authenticate}=require('../middleware/auth');r.get('/providers',c.providers);r.post('/compare',optional,v.compare,validate,c.compare);r.post('/reroute',authenticate,dv.reroute,validate,c.reroute);module.exports=r;