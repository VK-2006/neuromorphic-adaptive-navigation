const r=require('express').Router();const c=require('../controllers/journeyController');const {authenticate}=require('../middleware/auth');const v=require('../validators/domainValidators');const validate=require('../middleware/validate');r.get('/shared/:token',c.shared);r.use(authenticate);r.get('/',c.history);r.post('/',v.journeyCreate,validate,c.create);r.get('/:id',v.journeyIdParam,validate,c.get);r.post('/:id/start',v.journeyIdParam,validate,c.start);r.post('/:id/pause',v.journeyIdParam,validate,c.pause);r.post('/:id/resume',v.journeyIdParam,validate,c.resume);r.post('/:id/route',v.journeySwitch,validate,c.switchRoute);r.post('/:id/complete',v.journeyComplete,validate,c.complete);r.post('/:id/share',v.journeyShare,validate,c.share);r.delete('/:id/share',v.journeyIdParam,validate,c.revokeShare);r.get('/:id/replay',v.journeyIdParam,validate,c.replay);module.exports=r;