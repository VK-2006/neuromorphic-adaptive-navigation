const env=require('../config/env');const logger=require('../config/logger');async function sendEmail({to,subject,html}){if(!env.brevoApiKey||!env.brevoSenderEmail){logger.warn({event:'brevo_credentials_required',toDomain:String(to).split('@')[1]});return {mode:'credentials-required',sent:false}}const r=await fetch('https://api.brevo.com/v3/smtp/email',{method:'POST',headers:{'api-key':env.brevoApiKey,'content-type':'application/json',accept:'application/json'},body:JSON.stringify({sender:{email:env.brevoSenderEmail,name:env.brevoSenderName},to:[{email:to}],subject,htmlContent:html})});if(!r.ok)throw new Error(`Brevo error ${r.status}`);return {mode:'live',sent:true}}module.exports={sendEmail};
