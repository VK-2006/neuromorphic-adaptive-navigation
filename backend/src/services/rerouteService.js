const env=require('../config/env');const last=new Map();function shouldReroute(key,{criticalObstacle=false,verifiedBlockage=false,severeTraffic=false,deviationMeters=0,safetyDrop=0,alternativeGain=0}){const now=Date.now(),prev=last.get(key)||0;if(now-prev<env.rerouteCooldownSeconds*1000)return {reroute:false,reason:'cooldown'};let reason=null;if(criticalObstacle)reason='critical obstacle';else if(verifiedBlockage)reason='verified blockage';else if(severeTraffic)reason='severe traffic';else if(deviationMeters>80)reason='route deviation';else if(safetyDrop>.2)reason='route safety degradation';else if(alternativeGain>.15)reason='better alternative route';if(reason){last.set(key,now);return {reroute:true,reason}}return {reroute:false,reason:'stable'}}module.exports={shouldReroute};
