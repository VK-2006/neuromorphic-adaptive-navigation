const fs=require('fs'),path=require('path');const root=path.resolve(__dirname,'..','backend','src');let bad=0;for(const f of walk(root).filter(x=>x.endsWith('.js'))){try{require('child_process').execFileSync(process.execPath,['--check',f],{stdio:'pipe'})}catch(e){console.error('syntax',f,e.stderr?.toString());bad++}}function walk(d){return fs.readdirSync(d,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(path.join(d,e.name)):[path.join(d,e.name)])}process.exitCode=bad?1:0;console.log(`Checked ${walk(root).filter(x=>x.endsWith('.js')).length} backend files; failures=${bad}`);
