process.chdir(require('path').resolve(__dirname,'../backend'));const {connectDB}=require('../backend/src/config/db');const ChatRoom=require('../backend/src/models/ChatRoom');(async()=>{await connectDB();await ChatRoom.findOneAndUpdate({type:'GLOBAL',name:'Global'},{$set:{active:true}},{upsert:true});console.log('Demo room seeded');process.exit(0)})().catch(e=>{console.error(e);process.exit(1)});
